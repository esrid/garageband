bin/
*.db
*.db-shm
*.db-wal
*_templ.txt
.DS_Store

# secrets — GOOGLE_CLIENT_SECRET and friends live here, never in git
.env
.env.*

# test artifacts
*.out

# paperwork dropped in the working tree: carrier compliance bundles, signed
# forms. Business documents, often with identity data in them — not code.
*.pdf
